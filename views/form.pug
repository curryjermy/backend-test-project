doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title
    form(action='')
form(action='/form' method='post')
  label(for='crop name') crop name:
  input#crop-name(type='text' name='cropName' required='')
  label(for='crop weight') crop weight:
  input#crop-weight(type='number' name='cropWeight' required='')
  input(type='submit')
